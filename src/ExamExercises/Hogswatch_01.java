package ExamExercises;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Hogswatch_01 {
    public static void main(String[] args) throws IOException {
        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));

        int numOfHomesToVisit = Integer.parseInt(reader.readLine());
        int initialNumberOfPresents = Integer.parseInt(reader.readLine());

        //({initialPresents} / {visitedHomes}) * {remainingHomes} + {additionalPresents}
       // int numOfChildrenInHome = Integer.parseInt(reader.readLine());


    }
}
